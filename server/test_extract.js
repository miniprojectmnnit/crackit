const mongoose = require('mongoose');

// Need to quickly clear Page/Question tables to test cleanly if they aren't working
// Keeping this handy for clean integration testing.
console.log("Use normal tests via Postman/CURL");
