request is made to api/interviews/session to create a session
if url is passed or it is a dsa round then it need premium UI so redirect it to interview-room/(id or url)
else it fetch session and from that session it populates questions
